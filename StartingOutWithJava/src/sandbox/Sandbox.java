package sandbox;
import java.text.DecimalFormat;
import javax.swing.JOptionPane;

public class Sandbox {

	public static void main(String[] args) {
		
		//this is a comment to see if it is transmitted to the remote repository
		
		// find the cube
		
		/*final String sup = "\u00B3";		// holds superscript for 3
		String unit;						// holds the unit of measurement
		// declare double variables for length, width, height and volume
		double length, width, height, volume;
		
		// get length, width and height from the user, convert them to doubles and store them in variables
		length = Double.parseDouble(JOptionPane.showInputDialog(null, "Enter the length of your shape: "));
		width = Double.parseDouble(JOptionPane.showInputDialog(null, "Enter the width of your shape: "));
		height = Double.parseDouble(JOptionPane.showInputDialog(null, "Enter the height of your shape: "));
		unit = JOptionPane.showInputDialog(null, "Enter the unit of measurement: ");
		unit = unit + sup;
		
		// check to see if length and width are the same and that length and height are the same
		if( length == width && length == height ){
			// if they are all the same calculate the volume by using only the length value
			volume = Math.pow(length, 3);
			// let the user know the volume and that they have a cube
			JOptionPane.showMessageDialog(null, "Your shape is a cube and its volume is " + volume + " " + unit);
		}else{
			//else
			// calculate the volume by finding the product of length * width * height
			volume = length * width * height;
			// let the user know the volume
			JOptionPane.showMessageDialog(null, "Your shape's volume is " + volume + " " + unit);
		}*/
		
		int x = 10;
		while (x < 100);
		{
		x += 10;
		}

		
		System.out.print(x);
			
		
		
		
	}
}
